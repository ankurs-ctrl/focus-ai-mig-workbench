Understood — skipping the test file. Here's the frontend-only gap list, written up so you (or whoever applies it) can check it against your zip:

---

Frontend files to check for the 2.9.1 alias/SQL fix — missing pieces found when validating against the zip:

1. `frontend/src/features/sttm/mapping/relation-alias.ts`
   The core alias-generation functions (`aliasStem`, `identityDigest`, `relationAliasFor`) in the zip are an older version than what's currently running. This is the most important one to check — every other file's alias logic depends on this one function producing the exact same alias for the exact same relation. If your zip's version differs, pull the current file wholesale rather than trying to patch just the difference.

2. `frontend/src/features/sttm/source-target/table-relationship-flow.tsx`
   Missing a UI rename ("CTE" terminology → "Derived Source"/"DERIVED") plus a simplification of the join-editor logic (removed `joinDefinitionJoins`/`joinDefinitionTables`/`removeJoinDefinition` helpers in favor of using `activeTables`/`currentJoins` directly). This is unrelated to the alias/SQL bug — it's separate, already-shipped work — so if your zip is missing it, it's a real gap, just not one that's part of the alias fix itself.

3. `frontend/src/features/sttm/store/sttm-builder-slice.ts`
   Missing the "don't silently downgrade MAPPED rows to UNMAPPED" protection: `pendingSourceReconciliation` state, `sourceReconciliationAcknowledged` reducer, and the `wasMapped`/`downgradedTargetColumns` logic in the snapshot-restore path. Also unrelated to the alias/SQL bug specifically, but a real gap if missing — without it, a mapping row whose source relation can't be resolved on reload gets silently flipped to UNMAPPED instead of just being flagged for review.

4. `frontend/src/features/sttm/mapping/mapping-utils.ts`
   Not missing — flagging the other direction. Your zip's `buildFallbackSourceQuerySql` function has an extra change (using `relationAliasFor` + explicit `AS alias`) that isn't in the current repo at all. Worth a quick look to confirm it's intentional and correct, since it wasn't part of what I fixed or documented.

If any of these turn out to already be covered by other/later updates you've applied outside this zip, that's fine — this list is just what I found missing relative to the current repo state, not a claim that nothing else has changed since.
